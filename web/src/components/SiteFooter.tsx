export function SiteFooter() {
  return (
    <footer className="border-t border-[var(--color-line)] px-6 py-6">
      <div className="mx-auto flex max-w-5xl flex-col items-center justify-between gap-2 font-sans text-xs text-[var(--color-ink-faint)] sm:flex-row">
        <span>Keystone — Forward-Deployed Engineering, accelerated.</span>
        <span>Discovery through Solution Proposal, thirteen stages, one reviewed pack.</span>
      </div>
    </footer>
  );
}
