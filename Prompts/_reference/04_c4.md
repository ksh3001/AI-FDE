# Prompt 04 — C4 Architecture

Create C4 views for the proposed `{SYSTEM}`.

Include:

- people and external systems;
- the system boundary;
- independently deployable containers;
- the critical decision-workflow components;
- data, trust, privacy, authority and connectivity boundaries;
- offline or degraded-mode behavior;
- mapping between architecture elements and bounded contexts;
- prohibited operational write paths (`{PROHIBITED_ACTIONS}`).

Write outputs under `participant-outputs/04-c4/`.
